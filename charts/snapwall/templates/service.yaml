apiVersion: v1
kind: Service
metadata:
  name: {{ include "snapwall.fullname" . }}
  labels:
    {{- include "snapwall.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "snapwall.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
